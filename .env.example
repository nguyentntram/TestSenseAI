# Copy this file to .env and fill in real values when they exist.
# This Week 1 frontend scaffold does not read any of these yet —
# src/services/api.js uses mock data only. Documented here so the
# variable names are agreed on before the real backend exists.

# Base URL of the future REST API (API Gateway) once the backend exists.
VITE_API_BASE_URL=

# Future GitHub OAuth App client ID, once real GitHub auth is implemented.
VITE_GITHUB_CLIENT_ID=
